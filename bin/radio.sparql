PREFIX m: <http://example.org/music#>
PREFIX radio: <http://example.org/radios#>

SELECT ?track ?tracktitle ?artist
WHERE {
    ?track m:title ?tracktitle .
    ?track m:artist ?artist .
    radio:q-music m:plays ?track .
}
